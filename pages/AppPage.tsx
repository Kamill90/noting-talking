import Layout from '../components/Layout';

const AppPage = () => {
    return (
        <Layout isLandingPage={false}>
            {/* App page content */}
        </Layout>
    );
};

export default AppPage;